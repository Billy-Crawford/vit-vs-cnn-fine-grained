\section{Déploiement}
\label{sec:deployment}

% Rôle C — à rédiger (~0.5 page), une fois l'app finalisée avec les vrais modèles
%
% Points à couvrir :
% - Architecture de l'app : backend FastAPI (endpoints /predict, /attention)
%   + frontend Next.js.
% - Choix techniques : inference CPU (suffisant pour une image à la fois),
%   chargement des modèles une seule fois au démarrage.
% - Captures d'écran de l'app en action (upload, prédictions côte à côte,
%   heatmap d'attention).
% - (Optionnel) Lien de déploiement Hugging Face Spaces si réalisé.

TODO: rédiger une fois l'app finalisée avec les vrais modèles de B.

% \begin{figure}[t]
%   \centering
%   \includegraphics[width=\linewidth]{figures/app_screenshot.png}
%   \caption{Interface de démonstration : comparaison des prédictions ViT/ResNet.}
%   \label{fig:app_screenshot}
% \end{figure}
